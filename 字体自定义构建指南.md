# Maple Mono 字体自定义构建指南

## 需求
把斜体 `l` 改成非花体样式（去掉中间的尾巴）

---

## 基础知识

### 1. OpenType 特性（cvXX）

Maple Mono 内置了多个字符变体特性（Character Variants），其中与斜体 `l` 相关的是：

| 特性 | 作用 |
|------|------|
| `cv35` | **斜体 `l` 去除中间的尾巴**（和常规体一样） |

### 2. 为什么需要重新构建？

某些终端/IDE 不支持 OpenType 特性的动态启用，需要在构建时**冻结（freeze）**特性到字体文件中。

---

## 构建方式

### 方式一：使用 GitHub Actions（推荐）

#### 步骤 1: Fork 仓库
```
https://github.com/subframe7536/maple-font
```

#### 步骤 2: 修改 config.json

在 `feature_freeze` 部分设置：
```json
{
  "feature_freeze": {
    "cv35": "enable",  // 添加或修改这一行
    "cv04": "enable",  // cv04: l 底部带横杠（可选）
    // 其他特性保持默认...
  }
}
```

#### 步骤 3: 触发构建
1. 进入 **Actions** → **Custom Build**
2. 点击 **Run workflow**
3. 等待完成（约 5-10 分钟）
4. 在 **Releases** 下载字体

---

### 方式二：本地构建

```bash
# 1. 克隆仓库
git clone https://github.com/subframe7536/maple-font --depth 1 -b variable
cd maple-font

# 2. 安装依赖
pip install -r requirements.txt

# 3. 构建（启用 cv35）
python build.py --feat cv35
```

其他有用的构建选项：
```bash
# 构建普通版本（非"花体"风格，类似 JetBrains Mono）
python build.py --normal

# 构建 Nerd Font 版本（带图标）
python build.py --nf

# 构建中文字体版本
python build.py --cn

# 组合使用
python build.py --normal --nf --cn
```

---

### 方式三：在线构建（浏览器）

访问 https://font.subf.dev/en/playground ，点击左下角的 **Custom Build** 按钮。

---

## config.json 完整示例

```json
{
  "family_name": "Maple Mono",
  "feature_freeze": {
    "cv01": "ignore",
    "cv02": "ignore",
    "cv03": "enable",
    "cv04": "ignore",
    "cv35": "enable",    // 斜体 l 去除尾巴
    "cv33": "enable",    // 斜体 i j 带底部横杠
    "cv34": "enable",    // 斜体 k 去除中间圆圈
    "ss06": "enable",    // 去除斜体连笔
    "zero": "enable"     // 0 中间带点
  },
  "nerd_font": {
    "enable": true,
    "mono": true
  }
}
```

---

## 斜体相关特性一览

### 斜体独占特性（Italic Only）

| 特性 | 作用 |
|------|------|
| `cv31` | 斜体 `a` 顶部带横杠 |
| `cv32` | 斜体 `f` 去除底部尾巴 |
| `cv33` | 斜体 `i j` 带底部横杠和水平顶部横杠 |
| `cv34` | 斜体 `k` 去除中间圆圈 |
| `cv35` | **斜体 `l` 去除中间尾巴** ← 目标 |
| `cv36` | 斜体 `x` 去除顶部和底部尾巴 |
| `cv37` | 斜体 `y` 中间笔直交叉 |
| `cv38` | 斜体 `g` 双故事样式 |
| `cv39` | 斜体 `i` 去除底部横杠 |
| `cv40` | 斜体 `J` 去除顶部横杠 |
| `cv41` | 斜体 `r` 带底部横杠 |
| `cv42` | 斜体 `7` 带中间横杠 |
| `cv43` | 斜体 `Z z` 带中间横杠 |
| `cv44` | 斜体 `f` 带底部横杠 |

### 通用特性

| 特性 | 作用 |
|------|------|
| `cv04` | `l` 底部带横杠（像 Consolas） |
| `zero` | `0` 中间带点 |
| `ss06` | 去除斜体连笔（`al`, `ul`, `il` 等） |

---

## --normal 模式

使用 `--normal` 预置，构建出类似 JetBrains Mono 的风格（ slashed zero）：

```
cv01, cv02, cv33, cv34, cv35, cv36, cv61, cv62, ss05, ss06, ss07, ss08
```

---

## 字体格式说明

| 格式 | 说明 |
|------|------|
| Variable | 可变字重，文件小 |
| TTF | 建议使用！兼容性最好 |
| OTF | 可能有兼容性问题 |
| WOFF2 | 网页专用，体积小 |

**推荐下载**：`MapleMono-TTF` 或 `MapleMono-NF-TTF`（带图标）

---

## 相关链接

- **字体仓库**：https://github.com/subframe7536/maple-font
- **在线预览**：https://font.subf.dev/en/playground
- **特性文档**：https://github.com/subframe7536/maple-font/blob/dev/source/features/README.md
- **Nerd Font**：https://github.com/ryanoasis/nerd-fonts

---

## 待讨论

- [ ] 选择哪种构建方式
- [ ] 选择启用哪些特性
- [ ] 是否需要 Nerd Font 图标
- [ ] 是否需要中文字体版本
- [ ] 字体宽窄选择（default/narrow/slim）
